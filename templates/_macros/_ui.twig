{% macro setRootCategory(rootCategory, marginsize) %}
	{% set rootCss = 'muziek' %}
	{% if rootCategory.title == 'Cultuur' %}
		{% set rootCss = 'cultuur' %}
	{% endif %}

		<span class="category-base category-root {{ rootCss }} my-2 my-lg-0 me-{{ marginsize }} py-0 px-2">{{ rootCategory }}</span>

{% endmacro %}

{# {% macro setRootCategory(rootCategory) %}
	{% set rootCss = 'muziek' %}
	{% if rootCategory.title == 'Cultuur' %}
		{% set rootCss = 'cultuur' %}
	{% endif %}
	<div class="category-base category-root {{ rootCss }}">
		<a><span>{{ rootCategory }}</span></a>
	</div>
{% endmacro %} #}